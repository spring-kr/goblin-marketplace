# 🚀 Vercel 최적화 경량 requirements.txt
# 대용량 AI/ML 라이브러리 제거, 핵심 기능만 유지

# Flask 핵심
Flask==2.3.3
requests==2.31.0
beautifulsoup4==4.12.2
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7

# 날짜/시간 처리
python-dateutil==2.8.2

# 경량 문서 처리만 유지 (대용량 AI 라이브러리 제거)
PyPDF2==3.0.1
python-docx==0.8.11

# 제거된 대용량 패키지들 (Vercel 제한 초과 방지):
# pandas==2.0.3 (50MB+)
# openpyxl==3.1.2 (15MB+)
# python-pptx==0.6.21 (10MB+) 
# Pillow==10.0.0 (20MB+)
# pytesseract==0.3.10 (5MB+)
# nltk==3.8.1 (100MB+)
# torch (1GB+)
# transformers (500MB+)
# scikit-learn (50MB+)
# numpy (40MB+)
